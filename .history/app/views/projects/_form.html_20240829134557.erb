<%= form_with(model: project) do |form| %>
  <% if project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>
      <ul>
        <% project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>
<%#  %>
 <%# <div id="role-fields-container"></div>

<div class="field">
  <button type="button" id="add-role-btn">Add Role</button>
</div> %>
<div class="field">
  <%= form.label :user_id, 'Select Developer Users' %>
  <%= form.collection_select :user_ids, User.developer.where.not(id: current_user.id), :id, :email, {}%>
</div>

<%#  %>

  <%# <div id="users-fields"> %>
    <%# <%= form.fields_for :users do |user_fields| %> 
      <%# <% unless user_fields.object == current_user %> 
        <%# <div class="nested-fields"> %>
          <%# <%= render 'user_fields', f: user_fields %> 
        <%# </div> %>
      <%# <% end %> 
    <%# <% end %> 
  <%# </div> %>

  <%# <div>
    <button type="button" id="add-user-button">Add More User</button>
  </div> %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
